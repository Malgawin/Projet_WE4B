import { DateToPhrasePipe } from './date-to-phrase.pipe';

describe('DateToPhrasePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToPhrasePipe();
    expect(pipe).toBeTruthy();
  });
});
